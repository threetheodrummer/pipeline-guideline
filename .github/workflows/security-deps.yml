      - name: Run Trivy Dependency Scan
        run: |
          trivy fs \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --exit-code 1 \
            --format json \
            --output reports/trivy-deps.json \
            .
